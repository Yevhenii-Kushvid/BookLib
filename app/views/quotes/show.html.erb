<div>
  <p> <%= @quote.text %> </p>
  <%= content_tag :div , "likes: #{@total_quote_likes}", value: "#{like_book_quote_path()}", class: ( @i_can_like_it ? "like btn btn-info" : "like btn btn-danger"), style: "float: right;" %>
</div>


<% if @quote.user_id == current_user.id %>

<div class="btn btn-default">
  <%= link_to('Create', new_book_quote_path) %>
</div>
<div class="btn btn-default">
  <%= link_to('Edit', edit_book_quote_path)  %>
</div>

<div class="btn btn btn-danger">
  <%= link_to 'Destroy', book_quote_path(@quote.book_id, @quote.id), method: :delete %>
</div>

<% end %>
</td>
