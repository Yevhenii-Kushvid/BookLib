<%= form_for @quote, url: book_quotes_path, html: {multipart: true} do |f| %>
<%= errors @quote %>

<div class="field">
  <%= f.label "Quote #{:method}" %>
  <br>
  <%= f.hidden_field :id %>
  <%= f.text_area :text %>
</div>

<div class="actions">
  <%= f.submit %>
</div>

<% end %>
